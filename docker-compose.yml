version: '3'
services:
  express-service:
    build:
      context: ./
      dockerfile: Docker/Dockerfile-NodeBackend
    ports:
    - "3000:3000"
    links:
    - mongo
  mongo:
    build:
      context: ./
      dockerfile: Docker/Dockerfile-Mongo
